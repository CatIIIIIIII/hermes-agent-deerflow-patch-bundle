diff --git a/agent/auxiliary_client.py b/agent/auxiliary_client.py
index 13fb1c89..961577c9 100644
--- a/agent/auxiliary_client.py
+++ b/agent/auxiliary_client.py
@@ -194,6 +194,26 @@ _AI_GATEWAY_HEADERS = {
     "User-Agent": f"HermesAgent/{_HERMES_VERSION}",
 }
 
+
+def _custom_endpoint_headers(
+    base_url: str,
+    api_key: Optional[str] = None,
+    *,
+    is_vision: bool = False,
+) -> Dict[str, str]:
+    """Return safe default headers for custom OpenAI-compatible URLs."""
+    if base_url_host_matches(base_url, "ai-gateway.vercel.sh"):
+        return dict(_AI_GATEWAY_HEADERS)
+    if base_url_host_matches(base_url, "api.kimi.com"):
+        return {"User-Agent": "claude-code/0.1.0"}
+    if base_url_host_matches(base_url, "api.githubcopilot.com"):
+        from hermes_cli.copilot_auth import copilot_request_headers
+
+        return copilot_request_headers(is_agent_turn=True, is_vision=is_vision)
+    if base_url_host_matches(base_url, "chatgpt.com"):
+        return _codex_cloudflare_headers(api_key or "")
+    return {"User-Agent": "HermesAgent/1.0"}
+
 # Nous Portal extra_body for product attribution.
 # Callers should pass this as extra_body in chat.completions.create()
 # when the auxiliary client is backed by Nous Portal.
@@ -1171,9 +1191,12 @@ def _try_custom_endpoint() -> Tuple[Optional[Any], Optional[str]]:
     if custom_base.lower().startswith(_CODEX_AUX_BASE_URL.lower()):
         return None, None
     model = _read_main_model() or "gpt-4o-mini"
+    headers = _custom_endpoint_headers(custom_base, custom_key)
     logger.debug("Auxiliary client: custom endpoint (%s, api_mode=%s)", model, custom_mode or "chat_completions")
     _clean_base, _dq = _extract_url_query_params(custom_base)
     _extra = {"default_query": _dq} if _dq else {}
+    if headers:
+        _extra["default_headers"] = headers
     if custom_mode == "codex_responses":
         real_client = OpenAI(api_key=custom_key, base_url=_clean_base, **_extra)
         return CodexAuxiliaryClient(real_client, model), model
@@ -1663,6 +1686,10 @@ def _to_async_client(sync_client, model: str, is_vision: bool = False):
         )
     elif base_url_host_matches(sync_base_url, "api.kimi.com"):
         async_kwargs["default_headers"] = {"User-Agent": "claude-code/0.1.0"}
+    else:
+        default_headers = getattr(sync_client, "_default_headers", None)
+        if default_headers:
+            async_kwargs["default_headers"] = dict(default_headers)
     return AsyncOpenAI(**async_kwargs), model
 
 
@@ -1854,13 +1881,9 @@ def resolve_provider_client(
             _clean_base, _dq = _extract_url_query_params(custom_base)
             if _dq:
                 extra["default_query"] = _dq
-            if base_url_host_matches(custom_base, "api.kimi.com"):
-                extra["default_headers"] = {"User-Agent": "claude-code/0.1.0"}
-            elif base_url_host_matches(custom_base, "api.githubcopilot.com"):
-                from hermes_cli.copilot_auth import copilot_request_headers
-                extra["default_headers"] = copilot_request_headers(
-                    is_agent_turn=True, is_vision=is_vision
-                )
+            headers = _custom_endpoint_headers(custom_base, custom_key, is_vision=is_vision)
+            if headers:
+                extra["default_headers"] = headers
             client = OpenAI(api_key=custom_key, base_url=_clean_base, **extra)
             client = _wrap_if_needed(client, final_model, custom_base)
             return (_to_async_client(client, final_model, is_vision=is_vision) if async_mode
@@ -1900,6 +1923,9 @@ def resolve_provider_client(
                 )
                 _clean_base2, _dq2 = _extract_url_query_params(custom_base)
                 _extra2 = {"default_query": _dq2} if _dq2 else {}
+                headers = _custom_endpoint_headers(custom_base, custom_key, is_vision=is_vision)
+                if headers:
+                    _extra2["default_headers"] = headers
                 logger.debug(
                     "resolve_provider_client: named custom provider %r (%s, api_mode=%s)",
                     provider, final_model, entry_api_mode or "chat_completions")
diff --git a/run_agent.py b/run_agent.py
index 42f1e6f9..949c6891 100644
--- a/run_agent.py
+++ b/run_agent.py
@@ -1371,6 +1371,10 @@ class AIAgent:
                 elif base_url_host_matches(effective_base, "chatgpt.com"):
                     from agent.auxiliary_client import _codex_cloudflare_headers
                     client_kwargs["default_headers"] = _codex_cloudflare_headers(api_key)
+                elif self.provider == "custom":
+                    from agent.auxiliary_client import _custom_endpoint_headers
+
+                    client_kwargs["default_headers"] = _custom_endpoint_headers(effective_base, api_key)
             else:
                 # No explicit creds — use the centralized provider router
                 from agent.auxiliary_client import resolve_provider_client
@@ -5704,7 +5708,7 @@ class AIAgent:
         return True
 
     def _apply_client_headers_for_base_url(self, base_url: str) -> None:
-        from agent.auxiliary_client import _AI_GATEWAY_HEADERS, _OR_HEADERS
+        from agent.auxiliary_client import _AI_GATEWAY_HEADERS, _OR_HEADERS, _custom_endpoint_headers
 
         if base_url_host_matches(base_url, "openrouter.ai"):
             self._client_kwargs["default_headers"] = dict(_OR_HEADERS)
@@ -5725,6 +5729,11 @@ class AIAgent:
             self._client_kwargs["default_headers"] = _codex_cloudflare_headers(
                 self._client_kwargs.get("api_key", "")
             )
+        elif self.provider == "custom":
+            self._client_kwargs["default_headers"] = _custom_endpoint_headers(
+                base_url,
+                self._client_kwargs.get("api_key", ""),
+            )
         else:
             self._client_kwargs.pop("default_headers", None)
 
diff --git a/tests/agent/test_auxiliary_client.py b/tests/agent/test_auxiliary_client.py
index 825463eb..45bb7273 100644
--- a/tests/agent/test_auxiliary_client.py
+++ b/tests/agent/test_auxiliary_client.py
@@ -490,6 +490,29 @@ class TestExplicitProviderRouting:
 class TestGetTextAuxiliaryClient:
     """Test the full resolution chain for get_text_auxiliary_client."""
 
+    def test_custom_endpoint_uses_hermes_user_agent_for_generic_gateway(self, monkeypatch):
+        config = {
+            "model": {
+                "provider": "custom",
+                "base_url": "http://localhost:1234/v1",
+                "default": "my-local-model",
+            }
+        }
+        monkeypatch.setenv("OPENAI_API_KEY", "lm-studio-key")
+        monkeypatch.setattr("hermes_cli.config.load_config", lambda: config)
+        monkeypatch.setattr("hermes_cli.runtime_provider.load_config", lambda: config)
+
+        with (
+            patch("agent.auxiliary_client._read_nous_auth", return_value=None),
+            patch("agent.auxiliary_client._read_codex_access_token", return_value=None),
+            patch("agent.auxiliary_client._resolve_api_key_provider", return_value=(None, None)),
+            patch("agent.auxiliary_client.OpenAI") as mock_openai,
+        ):
+            get_text_auxiliary_client()
+
+        call_kwargs = mock_openai.call_args
+        assert call_kwargs.kwargs["default_headers"] == {"User-Agent": "HermesAgent/1.0"}
+
     def test_codex_pool_entry_takes_priority_over_auth_store(self):
         class _Entry:
             access_token = "pooled-codex-token"
diff --git a/tests/agent/test_auxiliary_named_custom_providers.py b/tests/agent/test_auxiliary_named_custom_providers.py
index 79f8b2f7..2c7ba39f 100644
--- a/tests/agent/test_auxiliary_named_custom_providers.py
+++ b/tests/agent/test_auxiliary_named_custom_providers.py
@@ -150,6 +150,23 @@ class TestResolveProviderClientNamedCustom:
         # Should use _read_main_model() fallback
         assert model == "main-model"
 
+    def test_named_custom_provider_uses_hermes_user_agent(self, tmp_path):
+        _write_config(tmp_path, {
+            "model": {"default": "main-model"},
+            "custom_providers": [
+                {"name": "beans", "base_url": "http://beans.local/v1", "api_key": "k"},
+            ],
+        })
+        with patch("agent.auxiliary_client.OpenAI") as mock_openai:
+            mock_openai.return_value = MagicMock()
+            from agent.auxiliary_client import resolve_provider_client
+
+            client, model = resolve_provider_client("beans")
+
+        assert client is not None
+        assert model == "main-model"
+        assert mock_openai.call_args.kwargs["default_headers"] == {"User-Agent": "HermesAgent/1.0"}
+
     def test_named_custom_no_api_key_uses_fallback(self, tmp_path):
         _write_config(tmp_path, {
             "model": {"default": "test"},
diff --git a/tests/run_agent/test_run_agent.py b/tests/run_agent/test_run_agent.py
index eb2b47f8..d55cf4ce 100644
--- a/tests/run_agent/test_run_agent.py
+++ b/tests/run_agent/test_run_agent.py
@@ -1258,6 +1258,14 @@ class TestBuildApiKwargs:
         kwargs = agent._build_api_kwargs(messages)
         assert kwargs["max_tokens"] == 4096
 
+    def test_apply_client_headers_sets_generic_custom_ua(self, agent):
+        agent.provider = "custom"
+        agent._client_kwargs = {}
+
+        agent._apply_client_headers_for_base_url("http://localhost:1234/v1")
+
+        assert agent._client_kwargs["default_headers"] == {"User-Agent": "HermesAgent/1.0"}
+
 
     def test_qwen_portal_formats_messages_and_metadata(self, agent):
         agent.base_url = "https://portal.qwen.ai/v1"
