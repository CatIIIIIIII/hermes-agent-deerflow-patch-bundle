diff --git a/agent/auxiliary_client.py b/agent/auxiliary_client.py
index 4860b16a..47f86c02 100644
--- a/agent/auxiliary_client.py
+++ b/agent/auxiliary_client.py
@@ -137,6 +137,19 @@ _OR_HEADERS = {
     "X-OpenRouter-Categories": "productivity,cli-agent",
 }
 
+
+def _custom_endpoint_headers(base_url: str) -> Dict[str, str]:
+    """Return safe default headers for generic custom OpenAI-compatible URLs."""
+    normalized = str(base_url or "").strip().lower()
+    if "api.kimi.com" in normalized:
+        return {"User-Agent": "KimiCLI/1.30.0"}
+    if "api.githubcopilot.com" in normalized:
+        from hermes_cli.models import copilot_default_headers
+
+        return copilot_default_headers()
+    return {"User-Agent": "HermesAgent/1.0"}
+
+
 # Nous Portal extra_body for product attribution.
 # Callers should pass this as extra_body in chat.completions.create()
 # when the auxiliary client is backed by Nous Portal.
@@ -991,11 +1004,13 @@ def _try_custom_endpoint() -> Tuple[Optional[OpenAI], Optional[str]]:
     if custom_base.lower().startswith(_CODEX_AUX_BASE_URL.lower()):
         return None, None
     model = _read_main_model() or "gpt-4o-mini"
+    headers = _custom_endpoint_headers(custom_base)
+    client_kwargs = {"default_headers": headers} if headers else {}
     logger.debug("Auxiliary client: custom endpoint (%s, api_mode=%s)", model, custom_mode or "chat_completions")
     if custom_mode == "codex_responses":
-        real_client = OpenAI(api_key=custom_key, base_url=custom_base)
+        real_client = OpenAI(api_key=custom_key, base_url=custom_base, **client_kwargs)
         return CodexAuxiliaryClient(real_client, model), model
-    return OpenAI(api_key=custom_key, base_url=custom_base), model
+    return OpenAI(api_key=custom_key, base_url=custom_base, **client_kwargs), model
 
 
 def _try_codex() -> Tuple[Optional[Any], Optional[str]]:
@@ -1324,6 +1339,10 @@ def _to_async_client(sync_client, model: str):
         async_kwargs["default_headers"] = copilot_default_headers()
     elif "api.kimi.com" in base_lower:
         async_kwargs["default_headers"] = {"User-Agent": "KimiCLI/1.30.0"}
+    else:
+        default_headers = getattr(sync_client, "_default_headers", None)
+        if default_headers:
+            async_kwargs["default_headers"] = dict(default_headers)
     return AsyncOpenAI(**async_kwargs), model
 
 
@@ -1499,12 +1518,8 @@ def resolve_provider_client(
                 model or _read_main_model() or "gpt-4o-mini",
                 provider,
             )
-            extra = {}
-            if "api.kimi.com" in custom_base.lower():
-                extra["default_headers"] = {"User-Agent": "KimiCLI/1.30.0"}
-            elif "api.githubcopilot.com" in custom_base.lower():
-                from hermes_cli.models import copilot_default_headers
-                extra["default_headers"] = copilot_default_headers()
+            headers = _custom_endpoint_headers(custom_base)
+            extra = {"default_headers": headers} if headers else {}
             client = OpenAI(api_key=custom_key, base_url=custom_base, **extra)
             client = _wrap_if_needed(client, final_model, custom_base)
             return (_to_async_client(client, final_model) if async_mode
@@ -1539,7 +1554,12 @@ def resolve_provider_client(
                     model or custom_entry.get("model") or _read_main_model() or "gpt-4o-mini",
                     provider,
                 )
-                client = OpenAI(api_key=custom_key, base_url=custom_base)
+                headers = _custom_endpoint_headers(custom_base)
+                client = OpenAI(
+                    api_key=custom_key,
+                    base_url=custom_base,
+                    **({"default_headers": headers} if headers else {}),
+                )
                 client = _wrap_if_needed(client, final_model, custom_base)
                 logger.debug(
                     "resolve_provider_client: named custom provider %r (%s)",
diff --git a/run_agent.py b/run_agent.py
index bb8cfa45..d82b0c1e 100644
--- a/run_agent.py
+++ b/run_agent.py
@@ -1034,6 +1034,10 @@ class AIAgent:
                     }
                 elif "portal.qwen.ai" in effective_base.lower():
                     client_kwargs["default_headers"] = _qwen_portal_headers()
+                elif self.provider == "custom":
+                    from agent.auxiliary_client import _custom_endpoint_headers
+
+                    client_kwargs["default_headers"] = _custom_endpoint_headers(effective_base)
             else:
                 # No explicit creds — use the centralized provider router
                 from agent.auxiliary_client import resolve_provider_client
@@ -5043,7 +5047,7 @@ class AIAgent:
         return True
 
     def _apply_client_headers_for_base_url(self, base_url: str) -> None:
-        from agent.auxiliary_client import _OR_HEADERS
+        from agent.auxiliary_client import _OR_HEADERS, _custom_endpoint_headers
 
         normalized = (base_url or "").lower()
         if "openrouter" in normalized:
@@ -5056,6 +5060,8 @@ class AIAgent:
             self._client_kwargs["default_headers"] = {"User-Agent": "KimiCLI/1.30.0"}
         elif "portal.qwen.ai" in normalized:
             self._client_kwargs["default_headers"] = _qwen_portal_headers()
+        elif self.provider == "custom":
+            self._client_kwargs["default_headers"] = _custom_endpoint_headers(base_url)
         else:
             self._client_kwargs.pop("default_headers", None)
 
diff --git a/tests/agent/test_auxiliary_client.py b/tests/agent/test_auxiliary_client.py
index 1778855d..1875e092 100644
--- a/tests/agent/test_auxiliary_client.py
+++ b/tests/agent/test_auxiliary_client.py
@@ -450,6 +450,27 @@ class TestExplicitProviderRouting:
 class TestGetTextAuxiliaryClient:
     """Test the full resolution chain for get_text_auxiliary_client."""
 
+    def test_custom_endpoint_uses_hermes_user_agent_for_generic_gateway(self, monkeypatch):
+        config = {
+            "model": {
+                "provider": "custom",
+                "base_url": "http://localhost:1234/v1",
+                "default": "my-local-model",
+            }
+        }
+        monkeypatch.setenv("OPENAI_API_KEY", "lm-studio-key")
+        monkeypatch.setattr("hermes_cli.config.load_config", lambda: config)
+        monkeypatch.setattr("hermes_cli.runtime_provider.load_config", lambda: config)
+
+        with patch("agent.auxiliary_client._read_nous_auth", return_value=None), \
+             patch("agent.auxiliary_client._read_codex_access_token", return_value=None), \
+             patch("agent.auxiliary_client._resolve_api_key_provider", return_value=(None, None)), \
+             patch("agent.auxiliary_client.OpenAI") as mock_openai:
+            get_text_auxiliary_client()
+
+        call_kwargs = mock_openai.call_args
+        assert call_kwargs.kwargs["default_headers"] == {"User-Agent": "HermesAgent/1.0"}
+
     def test_codex_pool_entry_takes_priority_over_auth_store(self):
         class _Entry:
             access_token = "pooled-codex-token"
diff --git a/tests/agent/test_auxiliary_named_custom_providers.py b/tests/agent/test_auxiliary_named_custom_providers.py
index 437a6c40..d9419fc0 100644
--- a/tests/agent/test_auxiliary_named_custom_providers.py
+++ b/tests/agent/test_auxiliary_named_custom_providers.py
@@ -130,6 +130,23 @@ class TestResolveProviderClientNamedCustom:
         # Should use _read_main_model() fallback
         assert model == "main-model"
 
+    def test_named_custom_provider_uses_hermes_user_agent(self, tmp_path):
+        _write_config(tmp_path, {
+            "model": {"default": "main-model"},
+            "custom_providers": [
+                {"name": "beans", "base_url": "http://beans.local/v1", "api_key": "k"},
+            ],
+        })
+        with patch("agent.auxiliary_client.OpenAI") as mock_openai:
+            mock_openai.return_value = MagicMock()
+            from agent.auxiliary_client import resolve_provider_client
+
+            client, model = resolve_provider_client("beans")
+
+        assert client is not None
+        assert model == "main-model"
+        assert mock_openai.call_args.kwargs["default_headers"] == {"User-Agent": "HermesAgent/1.0"}
+
     def test_named_custom_no_api_key_uses_fallback(self, tmp_path):
         _write_config(tmp_path, {
             "model": {"default": "test"},
diff --git a/tests/run_agent/test_run_agent.py b/tests/run_agent/test_run_agent.py
index 86f95580..f724ee03 100644
--- a/tests/run_agent/test_run_agent.py
+++ b/tests/run_agent/test_run_agent.py
@@ -2942,6 +2942,16 @@ class TestMaxTokensParam:
         assert result == {"max_tokens": 4096}
 
 
+class TestClientHeaders:
+    def test_apply_client_headers_sets_generic_custom_ua(self, agent):
+        agent.provider = "custom"
+        agent._client_kwargs = {}
+
+        agent._apply_client_headers_for_base_url("http://localhost:1234/v1")
+
+        assert agent._client_kwargs["default_headers"] == {"User-Agent": "HermesAgent/1.0"}
+
+
 # ---------------------------------------------------------------------------
 # System prompt stability for prompt caching
 # ---------------------------------------------------------------------------
